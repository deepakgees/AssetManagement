-- Run this from a different database (like 'postgres'), NOT from 'assetManagement':
SELECT pg_terminate_backend(pid)
FROM pg_stat_activity
WHERE datname = 'assetManagement_restore'
  AND pid <> pg_backend_pid();
